# Manual Prompt Baseline

Use this public-safe demo context:

- The fictional project is a small TypeScript application with separated apps, packages, docs, and fixtures.
- The agent should inspect files first, stay scoped, verify locally, and report clearly.
- Verification expectations include typecheck, unit tests, fixture validation, and smoke checks when behavior changes.
- Handoffs should capture changed work, verification, out-of-scope items, and next recommended phase.

Task: create a contractor handoff with project context, work rules, verification, out-of-scope boundaries, and next step.
