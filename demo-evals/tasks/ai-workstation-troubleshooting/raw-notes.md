# Raw Notes Baseline

Fictional workstation demo.

Hardware: high core-count desktop processor, large local development memory pool, single enthusiast GPU, separate system/project/model volumes.

Stack: local model server, web UI, coding-agent tools, Contextarr exports.

Network notes: prefer loopback defaults, document ports, LAN mode explicit.

Troubleshooting: capture symptom, affected layer, compare latest known working state, validate smallest likely fix, add reviewable note.

No real hostnames, addresses, model names, accounts, or private paths.
