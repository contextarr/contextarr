# Raw Notes Baseline

Project: fictional TypeScript app.

Areas: apps, packages, docs, fixtures.

Rules: inspect first, stay scoped, prefer existing patterns, verify locally, report clearly.

Workflow: current state -> smallest coherent change -> focused tests -> verification -> concise message.

Checks: typecheck, unit tests, fixture validation, smoke check depending on behavior.

Task brief should include goal, scope, likely files, work rules, verification, final report, stop condition.
