# Agent Kit Templates

This directory contains public-safe, data-only Agent Kit templates for Contextarr.

Templates are source files for creating local draft Agent Kits. They do not run Skills, execute commands, fetch remote data, publish to a registry, or include private data. Generated draft Agent Kits are written to the ignored `agent-kits/` output root and must be reviewed before they are treated as export-ready.
