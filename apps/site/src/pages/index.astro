---
import BaseLayout from "../layouts/BaseLayout.astro";
import Hero from "../components/Hero.astro";
import PackAnatomy from "../components/PackAnatomy.astro";
import Pipeline from "../components/Pipeline.astro";
import StatusSection from "../components/StatusSection.astro";
import SecurityBoundaries from "../components/SecurityBoundaries.astro";
import { futureDirectionLines, links } from "../content/site";
---

<BaseLayout currentPath="/">
  <Hero />
  <PackAnatomy />
  <Pipeline />
  <StatusSection />
  <SecurityBoundaries />

  <section class="section future-section" id="future">
    <div class="container split-layout">
      <div>
        <p class="eyebrow">Advanced preview</p>
        <h2>Skills and Agent Kits stay data-only while Context Packs stabilize.</h2>
        <p>
          These surfaces exist for local validation, indexing, read-only views, and export previews where implemented.
          The current adoption target is still the core Context Pack loop.
        </p>
      </div>
      <div class="future-lines">
        {futureDirectionLines.map((line) => <p>{line}</p>)}
      </div>
    </div>
  </section>

  <section class="section final-cta">
    <div class="container cta-panel">
      <p class="eyebrow">Built in public</p>
      <h2>Built in public.</h2>
      <p>
        Contextarr is being developed as an open-source, local-first project. Inspect the source, run the demo packs,
        and give feedback on the pack format.
      </p>
      <div class="hero-actions">
        <a class="button button-primary" href={links.github}>GitHub</a>
        <a class="button button-secondary" href="/security">Security</a>
        <a class="button button-quiet" href="/roadmap">Roadmap</a>
      </div>
    </div>
  </section>
</BaseLayout>
