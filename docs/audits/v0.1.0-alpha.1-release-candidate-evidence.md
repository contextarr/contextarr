# v0.1.0-alpha.1 Release Candidate Evidence

Evidence date: 2026-05-10

Status: local release candidate gate ready, not published.

## Scope

This note records the local alpha release path after adopting the Agentic AI Context Readiness and Local Observability PRD as a docs-only planning addition.

The release candidate remains local-only. It does not create a Git tag, GitHub release, package publish, deployment, registry entry, marketplace listing, hosted sync, or telemetry behavior.

## Latest Successful Gate

Branch: `codex/contextarr-overnight-alpha`

Result: Stage 1 release gate passed.

Current proof recorded for this release-candidate package:

- `pnpm release:verify` passed.
- `pnpm docs:verify` passed after the checkpoint.
- `pnpm screenshots:verify` passed after the checkpoint.
- Live Docker smoke passed on alternate port `http://127.0.0.1:33211` because an existing `contextarr-app-1` container occupied default port `3210`.
- The smoke container and smoke volume were cleaned up after verification.
- No push, tag, GitHub release, deployment, package publish, public registry action, marketplace action, or telemetry action was performed.

Live Docker smoke observations:

- Static UI loaded.
- `/api/health` returned ok.
- `authRequired` was `true`.
- Pack Library exposed 15 packs, including 12 starters.
- `ai-workstation` health was healthy.
- Review Queue showed 0 review items.
- Draft Intake showed 0 draft candidates.
- Collectors exposed 4 collectors.
- Codex export preview endpoint responded.
- Composer preview endpoint responded.

## Gate Command

```text
pnpm release:verify
```

Expected result before any public action:

- docs verification passes.
- screenshots verification passes.
- TypeScript typecheck passes.
- unit and integration tests pass.
- demo Context Packs validate.
- export, exposure, trust-loop, compatibility, security, Docker, UI, site, backup, and Composer gates pass.
- release docs verification passes.
- `git status --short --branch` is clean or contains only explicitly approved release metadata.

## Wave 1 Evidence Collector

Use the local collector to create a repeatable evidence packet from the current checkout and this recorded checkpoint:

```text
node tools/launch/collect-release-evidence.mjs
node tools/launch/collect-release-evidence.mjs --out docs/audits/v0.1.0-alpha.1-wave1-release-evidence.md
```

The packet includes:

- current git branch and commit.
- release gate status summary from this evidence note.
- Docker smoke port and observed results from this evidence note.
- screenshot manifest status from `docs/screenshots/v0.1.0-alpha.1/manifest.json`.
- no-public-action statement for the collector and release lane.

The collector does not run the release gate, start Docker, generate screenshots or video, push, tag, publish, deploy, create a GitHub release, update a registry or marketplace, or enable telemetry.

## Current Release Path

1. Keep `v0.1.0-alpha.1` as a draft release lane until Rob explicitly approves tagging or publishing.
2. Use `pnpm release:verify` as the required local proof before proposing any tag.
3. Re-run `pnpm docs:verify` and `pnpm screenshots:verify` after any release-package checkpoint that changes docs or screenshot references.
4. For Docker smoke, prefer `http://127.0.0.1:3210`; use an alternate local port when an existing `contextarr-app-1` occupies `3210`, and record the port used.
5. Keep Context Readiness and Local Observability as AR0 docs-only planning scope.
6. Keep Skills and Agent Kits as advanced-preview data-only surfaces.
7. Keep Draft Intake read-only: no approve, promote, activate, export, or MCP exposure of candidates.

## Manual Publish Path, Requires Explicit Approval

These actions are intentionally not performed by this release-candidate note:

```text
git tag -a v0.1.0-alpha.1 -m "v0.1.0-alpha.1"
git push origin v0.1.0-alpha.1
gh release create v0.1.0-alpha.1 --draft --notes-file RELEASE_NOTES.md
```

Before running those commands, confirm the local branch, remote target, final release notes, screenshots, and the no-publish/no-marketplace/no-telemetry boundaries.
