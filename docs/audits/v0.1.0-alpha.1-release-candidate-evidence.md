# v0.1.0-alpha.1 Release Candidate Evidence

Evidence date: 2026-05-10

Status: local release candidate gate ready, not published.

## Scope

This note records the local alpha release path after adopting the Agentic AI Context Readiness and Local Observability PRD as a docs-only planning addition.

The release candidate remains local-only. It does not create a Git tag, GitHub release, package publish, deployment, registry entry, marketplace listing, hosted sync, or telemetry behavior.

## Gate Command

```text
pnpm release:verify
```

Expected result before any public action:

- docs verification passes.
- TypeScript typecheck passes.
- unit and integration tests pass.
- demo Context Packs validate.
- export, exposure, trust-loop, compatibility, security, Docker, UI, site, backup, and Composer gates pass.
- release docs verification passes.
- `git status --short --branch` is clean or contains only explicitly approved release metadata.

## Current Release Path

1. Keep `v0.1.0-alpha.1` as a draft release lane until Rob explicitly approves tagging or publishing.
2. Use `pnpm release:verify` as the required local proof before proposing any tag.
3. Keep Context Readiness and Local Observability as AR0 docs-only planning scope.
4. Keep Skills and Agent Kits as advanced-preview data-only surfaces.
5. Keep Draft Intake read-only: no approve, promote, activate, export, or MCP exposure of candidates.

## Manual Publish Path, Requires Explicit Approval

These actions are intentionally not performed by this release-candidate note:

```text
git tag -a v0.1.0-alpha.1 -m "v0.1.0-alpha.1"
git push origin v0.1.0-alpha.1
gh release create v0.1.0-alpha.1 --draft --notes-file RELEASE_NOTES.md
```

Before running those commands, confirm the local branch, remote target, final release notes, screenshots, and the no-publish/no-marketplace/no-telemetry boundaries.
